# Default portfolio configuration
# Tickers and weights are fully customizable.
# Weights must sum to 1.0.

portfolio:
  name: "Default 10-Index Portfolio"

  holdings:
    - ticker: VOO # IVV, SPY, VTI considered.
      label: "US Large-Cap Equities"
      target_weight: 0.10
    - ticker: VO # AVUV, IJH, IJR, VB considered. AVUV too volatile.
      label: "US Small/Mid-Cap Equities"
      target_weight: 0.10
    - ticker: VEA # IEFA, SCHF considered.
      label: "Developed ex-US Equities"
      target_weight: 0.10
    - ticker: IEMG # VWO, SPEM considered.
      label: "Emerging Markets Equities"
      target_weight: 0.10
    - ticker: REET # SCHH, USRT, VNQ considered.
      label: "Global Real Estate"
      target_weight: 0.10
    - ticker: GLD # IAU, SGOL, GDX, GDXJ, RING, SGDM considered. If gold plummets, look at switch to GDX
      label: "Precious Metals/Gold"
      target_weight: 0.10
    - ticker: BCI # COM, DBC, PDBC considered.
      label: "Broad Commodities"
      target_weight: 0.10
    - ticker: IXC # VDE, XLE considered.
      label: "Energy/Resources"
      target_weight: 0.10
    - ticker: VGIT # IEF more aggressive, keep watch. AGG, BND, SCHZ considered.
      label: "Bonds/Fixed Income"
      target_weight: 0.10
    - ticker: USFR # BIL, SGOV, SHV, TFLO considered.
      label: "Cash/Short-Term"
      target_weight: 0.10

rebalance:
  # Scheduled rebalance: "2nd_wednesday" is the only supported value for now
  schedule: "2nd_wednesday"

  # Minimum calendar days between any two rebalance events (drift or scheduled)
  min_days_between_rebalances: 7

  drift:
    # Mode: "absolute" (percentage points) or "relative" (fraction of target weight)
    mode: "absolute"
    # Trigger rebalance when any holding drifts beyond this threshold from its target
    threshold: 0.075
